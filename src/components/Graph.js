import React from 'react';

const Graph = props => {
  console.log('data', props.data);
  //props.data is data for the graph
  return <React.Fragment>Awesome graph goes here</React.Fragment>;
};

export default Graph;
